[Standard]

number = true                          # ✅ Line numbers (IMPLEMENTED)

currentNumber = true                   # ✅ Current line number highlighting (IMPLEMENTED)

cursorLine = true                      # ✅ Cursor line highlighting (IMPLEMENTED)

statusLine = true                      # ✅ Show status line (IMPLEMENTED)

syntax = true                          # ✅ Syntax highlighting (IMPLEMENTED)

indentationLines = true                # ✅ Indentation guides (IMPLEMENTED)

tabStop = 2                            # ✅ Tab width (IMPLEMENTED)

expandTab = false                      # ✅ Insert spaces instead of tab (IMPLEMENTED)

sidebar = true                         # ✅ Show sidebar (IMPLEMENTED)

autoCloseParen = true                  # 📝 Auto-close parentheses (not yet applied)

autoIndent = true                      # 📝 Auto-indent (not yet applied)

ignorecase = true                      # 📝 Search case insensitive (not yet applied)

smartcase = true                       # 📝 Smart case search (not yet applied)

disableChangeCursor = false            # 📝 Cursor shape changes (not yet applied)

defaultCursor = "terminalDefault"      # 📝 Default cursor style (not yet applied)

normalModeCursor = "blinkBlock"        # 📝 Normal mode cursor (not yet applied)

insertModeCursor = "blinkIbeam"        # 📝 Insert mode cursor (not yet applied)

liveReloadOfConf = false               # 📝 Live config reload (not yet applied)

incrementalSearch = true               # 📝 Incremental search (not yet applied)

popupWindowInExmode = true             # 📝 Ex mode popup (not yet applied)

autoDeleteParen = false                # 📝 Auto-delete matching parens (not yet applied)

liveReloadOfFile = false               # 📝 Live file reload (not yet applied)

colorMode = "24bit"                    # 📝 Color mode: "8bit", "24bit", "none" (not yet applied)

[Clipboard]

enable = true                          # 📝 Clipboard integration (not yet applied)

tool = "xsel"                          # 📝 Tool: xsel, xclip, wl-clipboard, etc. (not yet applied)

[BuildOnSave]

enable = false                         # 📝 Build on save feature (not implemented)

# workspaceRoot = "/home/fox/git/moe"

# command = "cd /home/fox/git/moe && nimble build"

[TabLine]

allBuffer = false                      # 📝 Show all buffers in tab line (not yet applied)

[StatusLine]

multipleStatusLine = true              # ✅ Show status line per window (IMPLEMENTED)

merge = false                          # 📝 Merge status lines (not yet applied)

mode = true                            # 📝 Show mode in status line (not yet applied)

filename = true                        # 📝 Show filename in status line (not yet applied)

chanedMark = true                      # 📝 Show changed mark (not yet applied)

directory = true                       # 📝 Show directory (not yet applied)

gitChangedLines = true                 # 📝 Show git changed lines count (not yet applied)

gitBranchName = true                   # 📝 Show git branch name (not yet applied)

showGitInactive = false                # 📝 Show git info in inactive windows (not yet applied)

showModeInactive = false               # 📝 Show mode in inactive windows (not yet applied)

setupText = "{lineNumber}/{totalLines} {columnNumber}/{totalColumns} {encoding} {fileType}"  # 📝 Custom format (not yet applied)

[Highlight]

currentLine = true                     # 📝 Highlight current line (not yet applied)

reservedWord = ["TODO", "WIP", "NOTE"] # 📝 Highlight reserved words (not implemented)

replaceText = true                     # 📝 Highlight replace text (not implemented)

pairOfParen = true                     # 📝 Highlight matching parens (not yet applied)

fullWidthSpace = true                  # 📝 Highlight full-width spaces (not implemented)

trailingSpaces = true                  # 📝 Highlight trailing spaces (not implemented)

currentWord = true                     # 📝 Highlight current word (not implemented)

[AutoBackup]

enable = false                         # 📝 Auto-backup feature (not implemented)

# backupDir = "/home/user/.cache/moe/backups"

# 10 second
idleTime = 10

# 5 minute
interval = 5

dirToExclude = ["/etc"]

[QuickRun]

saveBufferWhenQuickRun = true          # 📝 Quick run feature (not implemented)

# command = "nimble build"

# 30 second
timeout = 30

# nimAdvancedCommand = "js"

# ClangOptions = "-Wall"

# CppOptions = "-Wall"

# NimOptions = "--debugger:native"

# shOptions = "-c"

# bashOptions = "-c"

[Notification]
# All notification settings are loaded but not yet applied to actual notification system

screenNotifications = true             # 📝 Screen notifications (not yet applied)

logNotifications = true

autoBackupScreenNotify = true

autoBackupLogNotify = true

autoSaveScreenNotify = true

autoSaveLogNotify = true

yankScreenNotify = true

yankLogNotify = true

deleteScreenNotify = true

deleteLogNotify = true

saveScreenNotify = true

saveLogNotify = true

quickRunScreenNotify = true

quickRunLogNotify = true

buildOnSaveScreenNotify = true

buildOnSaveLogNotify = true

filerScreenNotify = true

filerLogNotify = true

restoreScreenNotify = true

restoreLogNotify = true

lspScreenNotify = true

lspLogNotify = true

[Filer]

showIcons = true                       # 📝 File browser icons (not yet applied)

[Autocomplete]

enable = true                          # 📝 Autocomplete (not yet applied)

windowBorder = true                    # 📝 Autocomplete window border (not yet applied)

[AutoSave]

enable = true                          # 📝 Auto-save feature (not implemented)

interval = 5                           # 📝 Auto-save interval in minutes (not implemented)

[Persist]

exCommand = true                       # 📝 Persist command history (not implemented)

exCommandHistoryLimit = 1000

search = true

searchHistoryLimit = 1000

cursorPosition = true

[Git]

showChangedLine = true                 # ✅ Show git diff indicators in sidebar (IMPLEMENTED)

updateInterval = 1000                  # ✅ Git diff update interval in milliseconds (IMPLEMENTED)

[SyntaxChecker]

enable = false                         # ✅ Enable syntax checker in sidebar (IMPLEMENTED)

[SmoothScroll]

enable = true                          # 📝 Smooth scrolling (not yet applied)

minDelay = 5                           # 📝 Min delay in ms (not yet applied)

maxDelay = 20                          # 📝 Max delay in ms (not yet applied)

[StartUp.FileOpen]
autoSplit = true                       # 📝 Auto-split on startup (not implemented)
splitType = "vertical"                 # 📝 Split type: "vertical" or "horizontal" (not implemented)

# Debug sections - all settings are loaded but not yet applied
[Debug.WindowNode]
enable = true                          # 📝 Debug output (not yet applied)
currentWindow = true
index = true
windowIndex = true
bufferIndex = true
parentIndex = true
childLen = true
splitType = true
haveCursesWin = true
y = true
x = true
h = true
w = true
currentLine = true
currentColumn = true
expandedColumn = true
cursor = true

[Debug.EditorView]
enable = true
widthOfLineNum = true
height = true
width = true
originalLine = false
start = false
length = false

[Debug.BufferStatus]
enable = true
bufferIndex = true
path = true
openDir = true
currentMode = true
prevMode = true
language = true
encoding = true
countChange = true
cmdLoop = true
lastSaveTime = true
bufferLen = true

[Theme]

# 📝 Theme system not yet implemented
kind = "config"                        # 📝 Theme kind: "default", "config", "vscode" (not implemented)

# 📝 Please set the path of the theme config file.
path = "~/.config/moe/themes/dark.toml" # 📝 Theme file path (not implemented)
